# Node / Next.js
node_modules/
.next/
.vercel
*.tsbuildinfo
next-env.d.ts

# Models — git 추적 (빌드 시 필요, 총 30MB)
# client/paddle_models/ 는 commit 됨
# *.pdmodel / *.pdiparams 도 추적

# User data (사용자 PC 별 런타임 데이터 — git 추적 안 함)
client/logs/
client/data/contacts.json
client/data/schedules.json
client/data/send_queue_state.json
# templates/ 의 사용자 커스텀 파일도 제외, 단 시드/.gitkeep 은 강제 추가

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
build/

# Env / secrets
.env
.env.local
*.env
!.env.example
!admin/.env.production

# Local data
logs/
*.log
session.json

# PyInstaller
*.spec.bak

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Alembic 생성된 마이그레이션은 보존 — 단 빈 versions 디렉토리는 .gitkeep 으로 추적
